export enum Operator {
  EQUAL = "EQUAL",
  DIFFERENT = "DIFFERENT",
  BIGGER = "BIGGER",
  BIGGER_EQ = "BIGGER_EQ",
  SMALLER = "SMALLER",
  SMALLER_EQ = "SMALLER_EQ",
  // BETWEEN = "BETWEEN",
  NOT = "NOT",
  IN = "IN",
  // IS = "IS",
  REGEXP = "REGEXP",
}

export enum GroupOperator {
  AND = "AND",
  OR = "OR",
}
